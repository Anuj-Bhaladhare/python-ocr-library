from rotate_image import RotateImage
from resize_image import ResizeImage 
from crop_an_image import CropAnImage
from working_with_image_tuto import WorkingWithImageTutorial
from doc_tutorial import DocumentTutorial

if __name__ == "__main__":

    # ==================================================
    # Initilize Class
    # ==================================================
    rotate_image = RotateImage()
    resize_image = ResizeImage()
    crop_an_image = CropAnImage()
    working_with_image_tuto = WorkingWithImageTutorial()
    doc_tutorial = DocumentTutorial()


    # ==================================================
    # Call the classes Functions
    # ==================================================

    # rotate_image.deskiwImage()
    # resize_image.resize_img()
    # crop_an_image.crop_an_image()
    # working_with_image_tuto.resize_image()
    doc_tutorial.document_tutorial()

